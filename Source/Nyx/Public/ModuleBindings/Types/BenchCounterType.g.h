// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#pragma once
#include "CoreMinimal.h"
#include "BSATN/UESpacetimeDB.h"
#include "Types/Builtins.h"
#include "BenchCounterType.g.generated.h"

USTRUCT(BlueprintType)
struct NYX_API FBenchCounterType
{
    GENERATED_BODY()

    // NOTE: uint32 field not exposed to Blueprint due to non-blueprintable elements
    uint32 Id = 0;

    // NOTE: uint64 field not exposed to Blueprint due to non-blueprintable elements
    uint64 Value = 0;

    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "SpacetimeDB")
    FSpacetimeDBTimestamp LastUpdated;

    FORCEINLINE bool operator==(const FBenchCounterType& Other) const
    {
        return Id == Other.Id && Value == Other.Value && LastUpdated == Other.LastUpdated;
    }

    FORCEINLINE bool operator!=(const FBenchCounterType& Other) const
    {
        return !(*this == Other);
    }
};

/**
 * Custom hash function for FBenchCounterType.
 * Combines the hashes of all fields that are compared in operator==.
 * @param BenchCounterType The FBenchCounterType instance to hash.
 * @return The combined hash value.
 */
FORCEINLINE uint32 GetTypeHash(const FBenchCounterType& BenchCounterType)
{
    uint32 Hash = GetTypeHash(BenchCounterType.Id);
    Hash = HashCombine(Hash, GetTypeHash(BenchCounterType.Value));
    Hash = HashCombine(Hash, GetTypeHash(BenchCounterType.LastUpdated));
    return Hash;
}

namespace UE::SpacetimeDB
{
    UE_SPACETIMEDB_ENABLE_TARRAY(FBenchCounterType);

    UE_SPACETIMEDB_STRUCT(FBenchCounterType, Id, Value, LastUpdated);
}
