// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#pragma once
#include "CoreMinimal.h"
#include "BSATN/UESpacetimeDB.h"
#include "Types/Builtins.h"
#include "BenchTickLogType.g.generated.h"

USTRUCT(BlueprintType)
struct NYX_API FBenchTickLogType
{
    GENERATED_BODY()

    // NOTE: uint64 field not exposed to Blueprint due to non-blueprintable elements
    uint64 Id = 0;

    // NOTE: uint64 field not exposed to Blueprint due to non-blueprintable elements
    uint64 TickNumber = 0;

    // NOTE: uint32 field not exposed to Blueprint due to non-blueprintable elements
    uint32 EntitiesUpdated = 0;

    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "SpacetimeDB")
    FSpacetimeDBTimestamp Timestamp;

    FORCEINLINE bool operator==(const FBenchTickLogType& Other) const
    {
        return Id == Other.Id && TickNumber == Other.TickNumber && EntitiesUpdated == Other.EntitiesUpdated && Timestamp == Other.Timestamp;
    }

    FORCEINLINE bool operator!=(const FBenchTickLogType& Other) const
    {
        return !(*this == Other);
    }
};

/**
 * Custom hash function for FBenchTickLogType.
 * Combines the hashes of all fields that are compared in operator==.
 * @param BenchTickLogType The FBenchTickLogType instance to hash.
 * @return The combined hash value.
 */
FORCEINLINE uint32 GetTypeHash(const FBenchTickLogType& BenchTickLogType)
{
    uint32 Hash = GetTypeHash(BenchTickLogType.Id);
    Hash = HashCombine(Hash, GetTypeHash(BenchTickLogType.TickNumber));
    Hash = HashCombine(Hash, GetTypeHash(BenchTickLogType.EntitiesUpdated));
    Hash = HashCombine(Hash, GetTypeHash(BenchTickLogType.Timestamp));
    return Hash;
}

namespace UE::SpacetimeDB
{
    UE_SPACETIMEDB_ENABLE_TARRAY(FBenchTickLogType);

    UE_SPACETIMEDB_STRUCT(FBenchTickLogType, Id, TickNumber, EntitiesUpdated, Timestamp);
}
